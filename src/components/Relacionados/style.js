import styled from "styled-components";

const RelacionadosContainer = styled.div`
  margin-bottom: 120px;
`;

const Titulo = styled.h2`
  font-size: 1.75em;
  line-height: 1.925em;
  margin-bottom: 24px;
`;

const LerTambem = styled.div`
  display: flex;
  justify-content: space-between;
  gap: 24px;
  flex-wrap: wrap;
`;

export { RelacionadosContainer, Titulo, LerTambem }